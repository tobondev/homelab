# Mesh Node Provisioning Report
**Target Node:** `{{ inventory_hostname }}`
**Date of Execution:** {{ ansible_date_time.date }} at {{ ansible_date_time.time }}
**Execution Mode:** {{ 'DRY RUN (Render)' if 'render' in ansible_run_tags else 'PRODUCTION' }}

---

## Provisioning Summary

* **Status:** SUCCESS
* **Node Role:** `{{ node_role }}`
* **Batman-adv Mode:** `{{ 'client' if node_role == 'ap_only' else 'server' }}`

### Subsystem Configuration

* **Wireless:** Deployed via template
* **Firewall, DNS & DHCP:** {{ 'Deployed and Active (Local Routing)' if node_role == 'standalone' else 'Disabled (Delegated to Upstream)' }}
* **L3 Routing Architecture:** {{ 'Standalone (Local)' if node_role == 'standalone' else 'L2 Passthrough / Dumb-AP' }}

### Provisioned Logical Interfaces (VLAN Bridges)
* `br_{{ lan_interface }}` (VLAN: [REDACTED])
* `br_{{ guest_interface }}` (VLAN: [REDACTED])
* `br_{{ iot_interface }}` (VLAN: [REDACTED])
* `br_{{ tv_interface }}` (VLAN: [REDACTED])
* `br_{{ ctrl_interface }}` (VLAN: [REDACTED])

---

Generated by Ansible OpenWRT Provisioning Playbook.
