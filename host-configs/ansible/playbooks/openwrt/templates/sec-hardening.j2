# Security Hardening & Key Rotation Report
**Target Node:** `{{ inventory_hostname }}`
**Date of Execution:** {{ ansible_date_time.date }} at {{ ansible_date_time.time }}

---

## Authentication Rotation Summary

* **Status:** SUCCESS
* **Algorithm:** ED25519
* **Authorized Keys State:** Exclusive (1 key enforced)

### Deployed Public Key
```text
{{ staged_pubkey }}
```
**Root Password:** A new 64-character alphanumeric password was successfully generated in memory and applied to the node.
**Private SSH Key:** Generated locally and migrated to the host inventory directory.
**Security Notice:** > The plaintext root password and the private SSH key have been intentionally omitted from this artifact. The rot password is securely managed and encrypted via Mozilla SOPS within host_vars/{{ inventory_hostname }}.yml. 
The private SSH key is not uploaded into the repository as per .gitignore rules. This trade-off was accepted
due to the extreme technical complexity of managing sops-encryption-decryption of keys and the technical debt
of updating every single play.
The keys rotated have been manually encrypted with sops an put in a keys-dummy folder as proof of concetp

---

Generated by Ansible OpenWRT Security Hardening Playbook.
